// list of enums
//they cannot be created indside a class

enum TextSizes { small, medium, large }

enum OrderStaur { processing, shipped, delivered }

enum PaymentMethods { paypal, googlePlay, applePay, visa, masterCard, creditCard, paystack, razordPay, paytm }
